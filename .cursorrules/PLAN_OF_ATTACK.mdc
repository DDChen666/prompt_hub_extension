目的:
- 完成最小驗證（安裝/型檢/建置/單元），並固化流程與指紋。

範圍與受影響模組:
- 僅文檔與 .cursorrules；不改動應用程式碼。

測試點:
- `pnpm typecheck` 成功
- `pnpm build` 完成並生成 dist/
- `pnpm test:unit` 通過

觀察指標:
- 依賴安裝耗時、型檢錯誤計數、測試通過率

回退方案:
- 文檔變更可直接回退 git 變更；不涉及代碼邏輯。